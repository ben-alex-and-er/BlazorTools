@page "/"
@rendermode InteractiveServer


@using EFTables.Components
@using Radzen
@using Radzen.Blazor
@using Tables
@using Tables.Components
@using TestWebsite.Database


<PageTitle>Test</PageTitle>

<Table Items="dbUsers" PageSize="10">
	<Column Title="Name" Field="u => u.Name" Sortable="true" Filterable="true" />

	<Column Title="Email" Field="u => u.Email" Sortable="true" Filterable="true" />

	<Column Title="Created" Field="u => u.Created" Sortable="true" />

	<Column Title="Status" Field="u => u.Active" Sortable="true">
		<span class="badge @(context.Active ? "bg-success" : "bg-secondary")">
			@(context.Active ? "Active" : "Inactive")
		</span>
	</Column>
</Table>

<EFTable Query="@DbContext.Users" PageSize="10">
	<Column Title="Name" Field="u => u.Name" Sortable="true" Filterable="true" />

	<Column Title="Email" Field="u => u.Email" Sortable="true" Filterable="true" />

	<Column Title="Created" Field="u => u.Created" Sortable="true" />

	<Column Title="Status" Field="u => u.Active" Sortable="true">
		<span class="badge @(context.Active ? "bg-success" : "bg-secondary")">
			@(context.Active ? "Active" : "Inactive")
		</span>
	</Column>
</EFTable>



<RadzenDataGrid Data="@users" TItem="UserDto">
	<Columns>
		<RadzenDataGridColumn Property="@nameof(UserDto.Name)" Title="Name" />
		<RadzenDataGridColumn Property="@nameof(UserDto.Email)" Title="Email" />
		<RadzenDataGridColumn Property="@nameof(UserDto.Created)" Title="Created" />
		<RadzenDataGridColumn Property="@nameof(UserDto.Active)" Title="Active" />
	</Columns>
</RadzenDataGrid>